- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.vimrc: vimrc

- shell:
  - apt install fish curl
  - curl -L https://get.oh-my.fish | fish
  - omf install bobthefish
  - chsh -s $(which fish)
